# Zorva API (Express)

- Copy `.env.example` to `.env`
- Set `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY`
- Install deps at repo root: `npm install`
- Run dev: `npm run dev -w server`
- Healthcheck: `GET /health`